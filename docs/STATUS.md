# 交付状态与证据边界

截至2026-09-24，修复后的4次真实分析均 HTTP 200、应用校验通过。助手逐条复核确认本次等价方程方法、缺条件澄清、局部运算与只有答案的边界符合预期，仍记录了参考步骤配对不贴切的问题。两条照片结果由用户确认后保存至独立验证错题本，新进程恢复及重复确认不重复写入均通过；没有增加订正、自评或掌握标记。详见[本轮复测与保存记录](B_RETEST.md)。

本轮调用输入13,344、输出2,314、总计15,658 token，耗时合计12,568.86毫秒；实际费用未知。B累计19次真实请求，原账本13条通过、6条失败；这些混合任务计数不是准确率。复核和保存没有新增模型调用，人工评分仍为空。照片使用已确认的OCR和原图，未再次识图。

| 任务 | 已完成与当前边界 |
|---|---|
| A｜版本与入口 | 已核对新旧入口、版本和离线基线，保留私人数据 |
| B｜真实流程 | 文字分析—订正—确认保存—恢复已完成；两张合成图OCR—分析—确认收藏—恢复已完成；真实手机手写、更多题型与质量评测待做 |
| C｜工具循环 | 主入口及五场景联调脚手架完成离线测试；真实模型选工具尚未运行，是下一项 |
| D｜上下文 | 设置、本轮覆盖、同题追问与历史范围已接主流程；真实多轮教学效果待验证 |
| E｜对照评估 | 30条分组案例、开发集执行账本和评分表已备好；真实对照、保留集和人工评分未完成 |
| F｜交付 | 私有草稿PR、可复现源码和求职材料持续更新；最终质量报告待C/E证据 |

本次只保存已审阅结果并更新说明，没有改应用代码。最近应用全量633项、导出相关142项测试结果对应修复提交 `12da42d`；文档更新提交的CI以 Actions 为准。535份旧记录保持不变，照片存档中的原图与原始/确认转录恢复一致，重复确认后12份照片计划文件字节不变。

## 历史记录

下文保留各阶段当时的状态、计数及未验证项；当前状态以上表和 B_RETEST.md 为准。

2026-09-24 的任务B补充验证实际发送2次文字请求，第一组失败后整批停止，后两条照片分析未发送。b04 HTTP 200、当时格式通过，但将合法的方程等价方法标错；b05 HTTP 200、正确澄清缺少条件，却因空摘要/自检被校验器拒绝。两条均未保存。该次之后B历史累计为15次（原状态9条校验通过、6条失败），混合任务计数不是数学准确率。人工评分仍空、实际费用未知。

本轮离线修复允许有效澄清留空非必要的摘要/自检，并增加有界一元一次方程等价步骤检查，接入普通分析、订正、Agent、固定评估基线和保存校验。原始 b05 原样离线复核通过，b04 错误判断被阻止，旧状态与原文不改写。新版模型表现尚未实测。原4次/1元批次已停止，剩余2次不自动续发；此前E两题/10次/2元提案仍未批准。代码变化后旧冻结计划均不能直接运行，没有创建替代付费计划。详见 [分析证据检查](ANALYSIS_EVIDENCE.md)。

本轮全量 **633项测试通过（90.885秒）**，新增17项；172份白名单文件导出，导出包相关142项通过（19.840秒）。171份既有私人记录哈希不变，修复过程新增真实请求0次、保存0条。

以下保留先前各阶段的执行记录；其中冻结提案与通过数量对应各自当时版本。

E 的开发集真实执行入口已接上独立账本与预算预览，复用已有A/B服务和评分表。默认预览e03/e12两题、共4个教学回合、最多10次模型请求，新预留预算提案2元；尚未批准、未填写密钥、未发送新真实请求。每次请求先登记次数与金额估算，失败/中断后整份停止且不重发；实际费用仍保持未知。保留集仍关闭，人工评分继续暂缓。入口、核对来源与关键代码见 [AB_LIVE.md](AB_LIVE.md)。

开发集账本版全量 **616项测试通过（90.926秒）**，新增32项；170份白名单文件导出，导出包的新旧评估59项通过（9.491秒）。三个终端演练共12次本机HTTP请求：默认两题与多轮正常结束，故障计划1次后停止且剩余3回合未发送，新进程恢复状态不改记录。2441份已有私人文件（含前一轮E报告、源码导出和原55份受保护资料）哈希不变。新增真实请求为0。

阶段 E1–E2 已增加独立的30条合成评估用例：20开发、10保留，按15个数学题目家族划分，8条包含3–4轮交互。两版各47回合，共94行；0名真实学生。A使用合理教学要求、固定规则检索和最近1轮对话，B复用主程序有限工具循环与最多4轮上下文。双方使用相同题干、设置、资料权限、生成参数与输出校验，实际收到的资料和请求数分别记录。见[评估计划](../evaluation/agent_ab_v1/PLAN.md)与[逐回合标准](../evaluation/agent_ab_v1/RUBRIC.md)。

E1–E2 全量 **584 项测试通过（80.098秒）**，新增27项；165份白名单文件导出，导出包的新增27项通过（3.488秒）。完整本机演练使用101次手写HTTP请求：94行中91行完成、3行为预设工具故障/越权而停止，新增真实请求0次，人工分数为空。55份旧私人文件哈希不变。首次演练的手写占位反馈格式问题已修正并保留旧报告，没有据模型输出调参。

E1–E2 首轮只完成离线预览、本机HTTP演练和空白人工评分表。后续开发集账本已补齐（见上），E3–E5的真实开发集试跑、问题修订、保留集检查与人工质量报告仍未完成；新范围和预算需单独确认，旧调用额度不沿用。此评估不能作为已有准确率或教师评分写入简历。

阶段 D 已将三项长期设置、本轮覆盖、同题追问、待做任务、上下文预算及历史停用接入照片分析与订正主页面。普通请求与可选 Agent 共用同一实现；只做离线验证，新增真实 API 请求为 0。使用方法、保存位置和边界见 [CONTEXT.md](CONTEXT.md)。旧的 C 冻结计划因代码变化已失效且保留原样，尚未调用，不自动重建或沿用额度。

D 的最终全量 **557 项测试通过（80.077 秒）**，本轮新增 36 项；153 份白名单源码导出，导出包的新增 36 项通过（4.845 秒）。终端演练运行 3 次本机 HTTP 手写响应，本机页面已检查。24 份旧报告/存档和 31 份未执行 C 计划文件保持原样；这些结果不等于真实教学验收。

阶段 C 的真实联调准备已增加五场景冻结计划、累计 HTTP 名额、故障注入、无自动续跑和独立确认保存；当前新增真实请求仍为 0，调用范围与预算需用户另行确认。见 [AGENT_LIVE.md](AGENT_LIVE.md)。

此前工具联调准备版全量 **521 项测试通过（73.159 秒）**，其中本轮新增 22 项；Agent 相关 64 项通过。本机五场景演练使用 8 次手写 HTTP 响应，前四项完成，第五项按计划注入工具故障并停止。真实模型选择、引用质量和教学正确性仍待验证。

阶段 C 已完成有限只读工具循环的代码、主页面接入和离线测试：模型协议可直接回答或提出笔记/历史查询，程序控制权限、次数、时间、来源和用户确认。默认关闭，新增真实调用为 0；真实工具选择仍未验收，不能勾选 C 的真实模式验收项。运行方法与关键代码见 [AGENT_LOOP.md](AGENT_LOOP.md)。

工具循环首版的导出包全量 **499 项测试通过（62.771 秒）**，比此前 457 项增加 42 项；包含 Agent 协议、预算、工具失败、持久执行记录和主页面接受/放弃/订正。7 个终端场景共 10 次本机手写 HTTP 请求。142 份白名单源码可导出，旧报告和已保存记录的 24 份受保护文件哈希不变。GitHub CI 以对应提交的 Actions 为准。

第 2 项照片验证已完成两张合成图片的真实 OCR 和两次真实分析。OCR 保留学生错算并区分教师批注；分析均 HTTP 200、通过当时格式校验，参考答案均正确，但助手复核发现局部步骤标错、仅有答案却推断过程两处内容问题。两份分析尚未收藏，人工评分仍为空，真实手机手写仍待提供。详见[照片验证说明](PHOTO_SMOKE.md)。

此前照片分析的离线修复增加了只有答案时的固定反馈约束、其他文本中常见归因表述的检查，以及受限数值等式的局部判断；原始回复和已保存记录不改写，没有新增真实调用。新生成、接受和保存均检查规则，旧存档仍可读。新版实际模型表现尚未验证，不能勾选照片教学闭环；规则的支持范围见[分析证据检查](ANALYSIS_EVIDENCE.md)。

第 1 项最小文字闭环已完成：真实 b02 分析确认保存 → 真实 b06 回复 → 修复校验器误拒绝后离线复核原文 → 用户接受保存 → 新 Python 进程恢复。独立验证目录保留原分析副本并追加 1 条订正，版本为 2；重复确认未改变存档或决定日志字节，自评仍为空，没有标记掌握。日常错题本和旧失败记录不变。本轮保存没有新增模型调用。

B 阶段六份真实文字验证计划累计 9 次尝试，原始账本为 4 条应用校验通过、5 条失败、2 条原分析确认保存；另记历史回复离线复核后保存的订正 1 条。最新单次 b06 返回正确的 5/6、两步均正确，并说明旧约分本身正确；但把两条新步骤合并引用，触发旧校验器误拒绝。原记录保持 failed，不将复核改记成新的接口成功。累计输入 18,429、输出 6,331 token；人工评分为空，实际费用未知。

加上两次合成图片识图和两次图片分析，B 阶段累计 13 次真实调用；原始运行计数合计 8 条当时校验通过、5 条失败。任务不同，不能将该计数当作数学准确率；实际费用仍未知。照片计划两次额度已用完，检查结果未新增调用，未改原始报告或私人存档。

前一份 b06 把原本正确的约分标成“已订正”，那是真实证据矛盾，修复后仍拒绝。当前修复只允许引用横跨多条已评步骤，并要求引用的全部非空白字符都有对应判断；包含未评内容、错误/不确定步骤或冲突判断时仍拒绝。原文和状态不改写，没有新增付费请求。离线复核通过不是独立数学评测成绩，也不证明任意题目都正确。

strict 格式约束不能保证前后判断正确。已保存旧分析中的错误步骤生成订正 schema：只有这些完整引用才可以声明 corrected/still_incorrect，其他变化只可 changed/uncertain；仍独立检查本次步骤证据。历史验证使用 photo-correction-v5、study-strict-output-v2；其动态 schema 曾收到 HTTP 200 并返回符合结构的结果。当前提示词为 photo-study-v7 / photo-correction-v7、strict 协议为 study-strict-output-v3、分析证据规则为 analysis-evidence-v2；最新提示词及规则只做了离线验证。证据校验器为 correction-evidence-v2，真实原文的离线复核、用户确认保存与恢复均已完成。默认仍是 json_object；新增工具循环是独立可选分支，未改写这些历史验证，详见[文字验证记录](STUDY_SMOKE.md)。

验证入口提供冻结计划、本机 HTTP 演练、持久调用记录、失败停止和确认保存，复用页面相同的服务与存储。单题真实文字订正持久化已验收；普通页面已接入可选 Agent 循环，但仅做离线验证，真实照片仍未验收。本机演练不计作真实模型结果，第 1 项完成不表示整个 B 阶段或教学质量评估已完成。

现支持只读复用已确认 b02，冻结仅 b06 的一条订正请求。该单次计划已用完，禁止重试。另提供零模型调用的历史回复复核入口：只处理完整返回、来源可追溯且本次步骤证据失败的 strict 订正，保留原错误、原文哈希和新版校验代码。形成的新候选单独确认，接受后将原分析副本和订正一次保存到独立复核目录；拒绝、过期、来源变化不写错题文件，重复确认不重复追加。

已实现双图录入、裁剪旋转、核对、分析适配、原文证据校验、错题收藏、订正对照、确认或放弃、知识点回顾、导出和回收站。提供固定题完整离线演示，使用独立存档。

| 证据 | 可以支持 | 不能支持 |
|---|---|---|
| 单元、HTTP 和页面测试 | 协议、确认、去重、持久化与失败处理符合用例 | 任意数学题都讲对 |
| 固定题离线演示 | 无密钥检查完整交互与来源标识 | OCR 效果、真实模型时延与质量 |
| 13 张自写合成图片 | 已准备跨学段、错答、缺条件、模糊等场景 | 真实手机拍摄和手写识别能力 |
| 其中两张真实 OCR 调用 | 本次分数错算原文保留、学生答案与教师批注分离 | 其他 11 张效果、手机手写、解题诊断质量 |
| 两次图片真实分析 | 图片加 strict 的旧版本可返回结构化分析，发现了内容缺陷 | 当前修复版本效果、诊断可靠性、照片收藏闭环 |
| 原四题历史真实调用 | 旧版接口与回复校验曾运行，并发现过提示泄露 | 当前照片工作流或当前 prompt 的效果 |

前一版通过 303 项应用测试、12 项启动器测试和 11 项历史确认工具测试。本次新增完整演示与公开导出测试；公开版全量结果见 GitHub Actions 和 `release/verification.json`。

**真实 DeepSeek 照片识别、诊断和订正质量尚未验收，人工评分暂缓。** 没有准确率、学习提升、用户规模、留存或教师评分数据。空白评分表示未评分，不是 0 分或通过。

GitHub 仓库目前按用户要求设为私有。交付版本仍按 `tools/public_files.json` 筛选源码，保留测试依赖，排除本机私人路径、密钥、用户笔记与照片、原始调用报告和本地 Git 历史。`PUBLIC_MANIFEST.json` 的名称表示导出文件边界，不代表仓库当前对外可见。仓库的第一条提交是整理后的交付版本，不代表完整原始开发历史。

下一步：准备C的真实模型选工具验证，按当前提交重新核对五场景和预算；旧C/E冻结提案不直接运行，不沿用已完成的B调用额度。之后安排真实开发集对照、策略冻结、保留集和人工评分。D 的程序接入已完成，教学表现待检验。移动端、多题拆分与复习推荐再据证据决定。当前可以展示产品思考与可运行原型，不能描述为已规模化上线的智能教师。
